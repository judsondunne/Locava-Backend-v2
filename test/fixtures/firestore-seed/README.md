Deterministic Firestore seed assets for Backendv2 tests live here.

`seed-manifest.json` captures the canonical ids and surface coverage.
`test/firestore/seed-data.mts` generates the actual Firestore documents used by the emulator.
